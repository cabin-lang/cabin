/// The tags module, which handles tags on declarations.
pub mod tags;

/// The macros module, which has various utility macros.
pub mod macros;

/// The name module, which handles identifiers in the Cabin language.
pub mod name;

/// The types module, which handles traits for static type checking.
pub mod types;
