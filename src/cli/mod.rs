/// The commands module, which handles all of the different subcommands of the compiler, like `run` and `new`.
pub mod commands;

/// The theme module, which handles coloring preferences for the TUI.
pub mod theme;
