# Sourcing this file provides some helpful aliases
# Sorry Windows users I'd rather bash my head into a watermelon than figure out how batch works

# Alias "cabin" allowing commands like "cabin run", "cabin new", etc.
alias cabin="cargo run -q --"